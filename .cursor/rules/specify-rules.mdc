---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# Inssman Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-30

## Active Technologies
- TypeScript 5.3.x with React 18 + React, react-router-dom, tailwind-merge (003-search-enhancement)
- N/A (search is client-side filtering of in-memory data) (003-search-enhancement)
- `chrome.storage.local` via existing `StorageService` (004-resizable-columns)
- TypeScript 5.3.x (existing); compiles to ES output via Webpack + Babel + React 18 (UI only — not touched by this fix); `@types/chrome` 0.0.287; existing internal services (`StorageService`, `MatcherService`, `InjectCodeService`, `BaseService`/`ListenerService`) (005-fix-mock-hard-reload)
- `chrome.storage.local` (canonical for rules — not modified by this fix); page-world `window[NAMESPACE]` (read-only mirror exposed to interceptor) (005-fix-mock-hard-reload)

- TypeScript 5.x with React 18 + React, tailwind-merge, react-router-dom (002-sort-rules)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 5.x with React 18: Follow standard conventions

## Recent Changes
- 005-fix-mock-hard-reload: Added TypeScript 5.3.x (existing); compiles to ES output via Webpack + Babel + React 18 (UI only — not touched by this fix); `@types/chrome` 0.0.287; existing internal services (`StorageService`, `MatcherService`, `InjectCodeService`, `BaseService`/`ListenerService`)
- 004-resizable-columns: Added TypeScript 5.3.x with React 18 + React, react-router-dom, tailwind-merge
- 003-search-enhancement: Added TypeScript 5.3.x with React 18 + React, react-router-dom, tailwind-merge


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
